{
  "name": "root",
  "version": "1.0.0",
  "scripts": {
    "bootstrap-up": "pnpm -C @root/deployment run bootstrap-up",
    "bootstrap-down": "pnpm -C @root/deployment run bootstrap-down",
    "dev": "pnpm --filter @root/ui dev",
    "type-check": "pnpm -r type-check",
    "clean": "pnpm -r clean && rm -rf node_modules"
  }
}
